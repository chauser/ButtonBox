Add the Pi Pico libraries by adding the board type earlephilhower/arduino-pico.

Note that the joystick library interface is quite different from the joystick
library used in the matrix button box code (parent folder) which is for the
arduino pro micro and related boards.